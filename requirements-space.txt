# Hugging Face Space runtime deps
# Gradio is installed by the Space SDK (README sdk_version); do not pin it here.
# ds_rag_embedder/ is imported from the Space repo root (no editable "." install).
sentence-transformers>=3.0
transformers>=4.40
torch>=2.1
accelerate>=1.1.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
huggingface_hub>=0.26
datasets>=2.19
tqdm>=4.66
pyyaml>=6.0
plotly>=5.22
